import {Bebas_Neue} from "next/font/google";
import { Inter } from "next/font/google";

export const bebas = Bebas_Neue({weight: ['400'], subsets: []});
export const inter = Inter({subsets: ['latin']});
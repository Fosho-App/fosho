import { Inter, Big_Shoulders_Text,Bebas_Neue } from "next/font/google";

export const bebas = Bebas_Neue({weight: ['400'], subsets: []});
export const inter = Inter({subsets: ['latin']});
export const bigShouldersText = Big_Shoulders_Text({subsets: ['latin']});